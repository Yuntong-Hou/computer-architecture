Source PDF: /home/yuntongh/codex/agent-sandbox/lec4-paper-reading/sources/original-paper/network-on-memory-data-copy_ieee-cal20.pdf
GitHub URL: https://github.com/Yuntong-Hou/computer-architecture/blob/main/LEC4/original-paper/network-on-memory-data-copy_ieee-cal20.pdf
Raw URL: https://raw.githubusercontent.com/Yuntong-Hou/computer-architecture/main/LEC4/original-paper/network-on-memory-data-copy_ieee-cal20.pdf
Accessed: 2026-05-11 11:24:29 UTC



===== Page 1 =====

  IEEE COMPUTER ARCHITECTURE LETTERS                                                                                                                  1



  NOM: NETWORK-ON-MEMORY FOR INTER-BANK DATA TRANSFER IN HIGHLY-BANKED MEMORIES
    Seyyed Hossein SeyyedAghaei Rezaei 1                              Mehdi Modarressi 1,3  Rachata Ausavarungnirun 2
               Mohammad Sadrosadati3                                 Onur Mutlu 4    Masoud Daneshtalab 5
    1                        2                                                           3
    University of Tehran      King Mongkut's University of Technology North Bangkok       Institute for Research in Fundamental Sciences
                                            4                           5
                                             ETH Zürich                  Mälardalens University
  Abstract— Data copy is a widely-used memory operation in many programs and operating system services. In conventional computers, data copy
  is often carried out by two separate read and write transactions that pass data back and forth between the DRAM chip and the processor chip. Some
  prior mechanisms propose to avoid this unnecessary data movement by using the shared internal bus in the DRAM chip to directly copy data within
  the DRAM chip (e.g., between two DRAM banks). While these methods exhibit superior performance compared to conventional techniques, data
  copy across different DRAM banks is still greatly slower than data copy within the same DRAM bank. Hence, these techniques have limited benefit
  for the emerging 3D-stacked memories (e.g., HMC and HBM) that contain hundreds of DRAM banks across multiple memory controllers. In this
  paper, we present Network-on-Memory (NoM), a lightweight inter-bank data communication scheme that enables direct data copy across both
  memory banks of a 3D-stacked memory. NoM adopts a TDM-based circuit-switching design, where circuit setup is done by the memory controller.
  Compared to state-of-the-art approaches, NoM enables both fast data copy between multiple DRAM banks and concurrent data transfer operations.
  Our evaluation shows that NoM improves the performance of data-intensive workloads by 3.8X and 75%, on average, compared to the baseline
  conventional 3D-stacked DRAM architecture and state-of-the-art techniques, respectively.
       Index Terms— Memory Network, 3D-Stacked Memory, Circuit Switching, Data Copy, Memory Systems

  1 INTRODUCTION

T  HE memory subsystem is a major key performance bottleneck
   and energy consumer in modern computer systems. As a ma-
jor challenge, the off-chip memory bandwidth does not grow as
                                                                                (NoM). NoM carries out copy operations entirely within the
                                                                                memory across DRAM banks, without any intervention from the
                                                                                processor. To copy data across different banks, NoM adds a Time-
fast as the processor’s computation throughput. The limited                     division Multiplexed (TDM) circuit-switching mechanism to the
memory bandwidth is the result of relatively slow DRAM tech-                    3D-stacked DRAM memory controller. With NoM, banks in 3D-
nologies (necessary to guarantee low leakage), the non-scalable                 stacked memory are equipped with very simple circuit-switched
pinout, and on-board wires that connect the processor and                       routers and rely on a central node to set up circuits. This central-
memory chips.                                                                   ized scheme is compatible with the current architecture of 3D
   Prior work reports that a considerable portion of the memory                 memories because there already is a front-end controller unit that
bandwidth demand in many programs and operating system                          forwards all requests to their destination banks.
routines is due to bulk data copy and initialization operations [1].               In addition to its compatibility with the current structure of 3D
Even though moving data blocks that already exist in memory                     memories to improve speed of inter-bank data transfers, NoM’s
from one location to another location does not involve computa-                 second main advantage over prior in-memory data transfer archi-
tion on the processor side, the processor has to issue a series of              tectures [1-3] is its ability to perform multiple data transfers in par-
read and write operations that move data back and forth between                 allel. To perform data transfers in parallel, NoM replaces the global
the processor and the main memory.                                              links of the shared bus with a set of shorter inter-bank links and,
   Previous works address this problem by providing in-memory                   hence, yields higher throughput and scalability. Although there
data copy operations [1-5]. RowClone [1] provides a mechanism                   are some proposals to interconnect multiple 3D memory cubes
for both intra-bank and inter-bank copy, but RowClone’s main                    (chips) and processors via a network [6], to the best of our
focus is to enable fast data copy inside a DRAM subarray by mov-                knowledge, NoM is the first attempt to implement a network
ing data from one row to another through the row buffer inside                  across the banks of a 3D memory chip.
the same DRAM subarray [17]. As we show in Section 3, several                      Our experimental results show that NoM outperforms conven-
programs and operating system services copy data across DRAM                    tional 3D DRAM architecture and RowClone by 3.8x and 75%, re-
banks. In this case, RowClone uses the internal bus that is shared              spectively, under the evaluated copy-intensive workloads.
across all DRAM banks and moves data one cache block at a time.
During the copy, the shared internal DRAM bus is reserved and                     2 NETWORK-ON-MEMORY
other memory requests to the DRAM chip are therefore delayed.
   Emerging 3D-stacked DRAM architectures integrate hundreds                       Baseline 3D DRAM architecture. Although NoM can be used
of banks in a multi-layer structure. Data copy in 3D-stacked DRAM               in both traditional DRAM and 3D-stacked DRAM architectures,
is much more likely to occur across different DRAM banks.                       we specifically tailor our design for the emerging 3D-stacked
RowClone does not provide as high benefits as for intra-bank copy               memories, like the Hybrid Memory Cube (HMC) [7]. In the HMC
when copying data across banks since it leverages the low-band-                 architecture, up to 8 DRAM layers (for a total of 8GB capacity)
width shared bus between banks to move data. Aside from the                     are stacked on top of one logic layer. Each layer (either logic or
overhead of cross-bank data copies, DRAM banks of the 3D mem-                   DRAM) is divided into 32 slices. Each DRAM slice contains two
ories are partitioned into several sets, each set having its own inde-          DRAM banks. The logic and DRAM slices that are vertically ad-
pendent memory controller. RowClone’s design does not provide                   jacent form a column of stacked slices, called a vault [7]. Each
for data movement between independently-controlled banks. While                 vault has its own vault controller implemented on its logic die.
LISA [3] improves RowClone’s performance by supporting faster                   Vaults can be accessed simultaneously. Each vault consists of
inter-subarray copies, it does not provide any improvement for in-              several (e.g., 4-8) banks that share a single set of internal data,
ter-bank copies.                                                                address, and control buses. Each bank contains tens of (e.g., 64-
   To allow direct data copy between memory banks in 3D-stacked                 128) subarrays, each of which consists of hundreds of (e.g., 512-
DRAM, we propose to interconnect the banks of a highly-banked                   2048) rows of DRAM cells that share a global row buffer that en-
3D-stacked DRAM using a lightweight network-on-memory                           ables access to data in a given row.

                                            xxxx-xxxx/0x/$xx.00 © 200x IEEE   Published by the IEEE Computer Society



===== Page 2 =====

 2                                                                                                                                                                      IEEE CAL



   NoM architecture. In addition to the conventional address,             NoM Links Data/Address/Control Bus
data, and control buses, NoM connects each bank to its neighbor-                                                                                Subarray




                                                                                                                               Row Decoder
                                                                            Bank                                                                                               CS Buf. #1
ing banks at the X, Y and Z dimensions to form a 3D mesh topol-




                                                                                                                Address Bus
ogy. We use the mesh topology due to its simple structure and




                                                                                                                                                                                            NoM Links
                                                                                                                                                                               CS Buf. #n
                                                                                                                                                Subarray
short non-intersecting links, which do not significantly change                                                                              Global Row Buffer
the structure of the DRAM layer.                                                                        Bank                                  Column Dec            Crossbar

  We show the high-level organization of a 2D DRAM chip (for                                                                                                     Ctrl
                                                                                                                                                   Data
simplicity) with NoM in Fig. 1a. The dashed lines represent the




                                                                                                                Data Bus
extra links added to establish a network. NoM adopts TDM-                                                                                          MUX

based circuit-switching [8]. In a TDM network, each circuit re-           DRAM           Chip I/O                             CS Buf.: Circuit-switching buffer
serves one or multiple time slots in a repeating time window.                                                                  Ctrl: Controller and TDM slot table
With n-cycle time windows, each router has an n-entry slot table,                          (a)                                                             (b)
which is filled by the circuit control unit during circuit setup. This   Fig. 1. (a) DRAM structure with NoM links, and (b) The modified architecture of a
table determines the input-output connections of the router at           memory bank. The components that are unique to NoM are colored in yellow
each cycle of the time window (see Section 2.1). To manage this
circuit switch, NoM introduces a centralized circuit control unit        to the destination in parallel. This hardware accelerator is com-
(CCU). The CCU receives internal data transfer commands from             posed of a matrix of simple processing elements (PEs), each asso-
the processor and finds and reserves a circuit, as a sequence of         ciated with a network node. Assuming that the circuit reservation
TDM time slots, from the source to the destination banks.                is done on n-slot windows and each router has p output ports,
   In most 3D-stacked memories, including the HMC [7], there             each accelerator PE keeps the occupancy state of its correspond-
are two levels of logic between the processor and memory banks:          ing network node in a 2-D matrix V of size p×n. V[i][j]=1 indicates
(1) the front-end controller of the memory chip that directs pro-        that the jth slot of the ith output port of the corresponding network
cessor requests to the corresponding vault via a crossbar and (2)        router is reserved. To find a path, the source PE (associated with
vault controllers, each of which acts as the memory controller for       the source node of the circuit) uses an n-bit vector with all ele-
the banks within the vault. Since the front-end controller has a         ments initialized to zero. This bit vector keeps track of the empty
global view of all memory transactions, the CCU can be added             time slots of different paths and is propagated through all the
directly to the front-end controller.                                    shortest paths to the destination. If time slot x is free in this router,
   Fig. 1b shows the architecture of a bank in more detail. The          we need time slot x+1 to be free in the next router. Hence, in each
components unique to NoM are colored in yellow. The bank, as             PE, the bit vector is first rotated right and then ORed with the
the figure indicates, can send/receive data to/from either 1) the        vectors corresponding to the output ports along the shortest path
network through the network links/circuit switching buffers (CS          to eliminate (mark as unavailable) busy slots from the bit vector.
Buf) for direct data transfer operations or 2) the conventional          It is then passed to the next PE towards the destination cell.
buses via the bank IO buffer for regular read/write operations.          Available circuit paths (sequence of time slots) appear as zero bits
NoM’s circuit-switched router is simple and only consists of a           in the vector at the destination PE. The circuit path and time slots
crossbar, a latch (associated with each network link to keep in-         can be reserved by tracing back the path towards the source PE.
coming data for a single cycle), and a local controller unit. This         With B-bit links, B bits of data is transferred on NoM link in
local controller unit (Ctrl) is simply a time-slot table that deter-     each cycle. If a circuit has V bits to transfer, the time-slots remain
mines the input-output connections of NoM ports (via the inter-          reserved for V/B time windows. The data transfer can be acceler-
nal crossbar of the bank) on a per-cycle basis. The time-slot table      ated by reserving multiple slots, provided that the algorithm re-
is programmed by the CCU. These components are all compati-              turns more than one free slot. After that, the algorithm is allowed
ble with the current DRAM technology and can be integrated into          to use the time-slot for the next requests.
off-the-shelf 3D DRAM chips (Note that current DRAM chips
already have crossbars and latches). The circuit-switched router          2.2 Data Transfer on NoM
does not need the complex logic that a typical packet-switched
                                                                             Fig. 2 shows the implementation of NoM on a 3D-stacked
router employs for buffering, routing, arbitration, hop-by-hop
                                                                         HMC-like DRAM architecture. The operation of the network is
flow control, and VC allocation [8]. Thus, circuit-switching has
smaller routers and is faster than packet-switching (e.g., it has        divided into two steps: circuit setup and data transfer over the
one hop per cycle packet traversal).                                     circuit. Similar to prior work, we assume that the processor issues
                                                                         a specific direct data copy request that is separate from the regu-
 2.1 TDM Slot Allocation                                                 lar read/write requests [1-5]. A direct data copy is handled by the
   The NoM circuit setup is done in the CCU in a centralized man-        CCU as follows:
ner. The CCU keeps the state of all reserved time slots across the          1. The CCU queues and services the direct data copy requests
network and services a new request by finding a sequence of time         in a FIFO manner. For each request, a circuit is established be-
slots along one of the paths between the source and destination          tween the source and destination banks using the TDM slot allo-
banks. The TDM slot allocation is a complex task, as it should           cation logic (See Section 2.1). Fig. 2 shows an example of this op-
guarantee collision-free movement of data.                               eration. In Fig. 2, there is a direct data copy request from bank A
   Specifically, the allocation must guarantee that (1) no time slot     to bank B, arriving at the CCU at time t. Assume that NoM has 8-
of a link is shared by multiple circuits, and (2) a circuit must use     slot windows and the currently active time slot (based on which
increasingly numbered slots in consecutive routers to avoid data         the router connections are configured) is time slot 0. Fig. 2 shows
buffering. For example, if time slot m is allocated to a circuit in a    an example circuit between bank A and bank B that comprises of
router, time slot m+1 should be allocated to it in the next router       five consecutive time slots in five routers, starting from time slot
to have the data advance one hop per cycle without any need for          3 (as the starting slot of the earliest available circuit) at the router
buffering, arbitration, and hop-by-hop flow control.                     associated with bank A.
   NoM utilizes the fast and efficient centralized circuit setup            2. The CCU sends a read request to the source vault controller
mechanism presented in prior work [9]. NoM relies on a hard-             to generate the data read signals for the target block at the source
ware accelerator to explore all the possible paths from the source       bank A at time slot 3.



===== Page 3 =====

 SEYEDAGHAEI ET AL.: NOM                                                                                                                                                               3



                                                                                                                 Memory consistency should be enforced by software, i.e. soft-
                                                                                                                 ware should insert special synchronization instructions when
                       DRAM Layer
                                                                                                                 it is necessary to enforce order or atomicity [1].
                                                                            Time Input Output
  L : Local Port              Time Input Output                                 3       L           S
                                 4    N       S    A
  W
        U
            N                  Time Input Output
                                                                                                                  2.3 NoM Implementation
  S             E               5    N       D                    Time Input Output                                 We implement NoM on an HMC-like 3D-stacked memory.
        D                                                               6       U           D                    Each bank is connected to up to 6 other banks along the three
      Vault                                                             Time Input Output                        dimensions. The link width is set to the internal memory bus
                                              B                             7       U           L                width, i.e. 64 bits. Short planar links are used to connect two ad-
                                                                                                                 jacent banks in a layer. For the vertical dimension, HMC already
                         Logic Layer                                                                             has a set of vertical links, implemented with the Through Silicon
Vault Controller                                                                                                 Via (TSV) technology. A TSV carries address, data, and control
                                                   Central Controller
                                                                                Data                             signals between the vault controller and DRAM banks. NoM
                    Crossbar                                                                                     with a full 3D mesh topology needs some extra TSVs to imple-
                                                     and Crossbar
                                                                                                      Vault
                                                                                        Copy Q
                                                                                                    Controller   ment 3D mesh links in the third dimension (which connect every
                                                                                                     +TSVs
                           Circuit Control
                                                                         Ctrl/
                                                                        Address
                                                                                                                 pair of vertically adjacent banks). To reduce the area overhead of
                                                                                        R/W Q
                            Unit (CCU)                                                                           NoM’s full 3D mesh, we also propose a low-overhead design
                                                                                                                 called NoM-Light. This design eliminates the 3D mesh vertical
      Fig. 2. NoM on 3D DRAM and an example circuit from bank A to bank B                                        links but shares the bandwidth of the already existing TSVs to
                                                                                                                 perform data transfer vertically. This design is motivated by our
  3. The CCU waits for data to traverse the circuit and reach the                                                observation that the probability of simultaneously using both ex-
destination. CCU knows when the data arrives at the destination                                                  isting TSVs and NoM’s 3D mesh TSVs in a single cycle in NoM
bank because circuit-switching has deterministic transmission                                                    with full 3D mesh topology is 0.45% under low NoM load and
time.                                                                                                            7.1% under high NoM load. This low conflict rate suggests that
   4. Finally, the CCU sends a write request to the destination                                                  we can eliminate the full 3D mesh TSVs and use the conventional
vault controller to write the received block to the destination                                                  HMC TSVs also for transferring NoM data with no noticeable
bank B at time slot 7.                                                                                           performance loss.
   Once a request is picked up at cycle t, the CCU takes three cy-                                                  A TSV that carries the address/data signals of a vault is a bus
                                                                                                                 (and not a point-to-point link as in a full 3D mesh), so NoM’s data
cles to route the request (one cycle to find a path, one cycle to
                                                                                                                 is transferred in a broadcast-based manner vertically [10]. The
establish the circuit path by configuring the slot tables along the                                              disadvantage of this NoM-Light design, compared to the NoM
circuit path, and one cycle to issue the read request and make                                                   with a full 3D mesh, is that only a single data item can traverse
data ready). So, the earliest time that the algorithm can consider                                               the third dimension in each vault simultaneously. The advantage
for the data transfer on the circuit to start is t+3. CCU updates the                                            of the NoM-Light design, however, is that NoM data can traverse
slot tables by using dedicated links.                                                                            any number of hops in the third dimension in a single cycle. As
   In our design, all vault controllers can serve both regular                                                   the vertical links are very short, this single-cycle multi-hop tra-
memory accesses and DRAM refresh requests in parallel with                                                       versal has no timing violation problem [11-12].
NoM’s data copy operations. The only exception is when the cor-                                                     NoM uses special extra sideband TSVs to program the slot ta-
responding vault controllers are busy reading and writing NoM                                                    bles. In each cycle, CCU sets at most one slot table entry in each
                                                                                                                 vault. All slot tables are connected to a shared vertical link. The
data at the source and destination banks, respectively. In between
                                                                                                                 link is 12 bits wide to set the right slot: 3 bits to select one of the 8
these read and write steps, the data block passes on the NoM                                                     banks of the vault, 4 bits to select a slot in the 16-slot window,
links and leaves the read/write bandwidth idle for regular ac-                                                   and 6 bits to carry the slot table data: 3 bits to select one of the six
cesses. NoM’s ability to service concurrent copy operations as                                                   input ports and 3 bits to select one of the six output ports that
well as other regular memory accesses is one of its main ad-                                                     should be connected at that slot.
vantages over previous works [1, 3].
   A vault controller, as shown at the bottom right of Fig. 2, stores                                             3 EVALUATION
regular requests received from the front-end memory controller
                                                                                                                    Simulation environment. We use Ramulator [13], a fast and
in a regular queue (R/W Q) and read/write requests related to
                                                                                                                 extensible open-source DRAM simulator that can support differ-
direct copy requests to a high-priority queue (Copy Q). Direct
                                                                                                                 ent DRAM technologies, to evaluate the effectiveness of NoM.
copy requests are distinguished with a flag set by CCU.
                                                                                                                 We measure system performance using instructions per cycle
   From the circuit-switched router’s point of view, the entire pro-
                                                                                                                 (IPC). Circuit-level parameters and memory timing parameters
cess is very simple. The circuit-controller reads the slot table en-
                                                                                                                 are set based on DDR3 DRAM [15]. The baseline target memory
try for each time slot, shown by the <t,IO> tuples in Fig. 2, and
                                                                                                                 is a 4GB HMC-like architecture with 32 vaults, four DRAM lay-
connects the input-output (I-O) pair indicated by the tuple using
                                                                                                                 ers, and two banks per DRAM slice (for a total of 256 banks). The
the crossbar. At the source and destination banks, circuit-control-
                                                                                                                 NoM topology is an 8×8×4 mesh. The circuit-switching time win-
ler also sets up the multiplexer inside the bank (MUX in Fig. 1b)
                                                                                                                 dow has 16 time-slots. All datapaths and links inside the memory
to exchange data with memory. For example, at time slot 7, the
                                                                                                                 are 64 bits wide. We integrate the intra-subarray and intra-bank
slot table connects port U to port L (local ejection port) to forward
                                                                                                                 direct data copy mechanisms of RowClone [1] and LISA [3] into
the data received from input port U to output port L, and from
                                                                                                                 NoM: this way, inter-bank data copy is carried by NoM, whereas
there, to the data buffer of the bank.
                                                                                                                 intra-subarray/bank data copy is handled by RowClone/LISA.
    Correctness. We handle memory consistency and cache coher-
                                                                                                                 We compare NoM to two baselines, RowClone and 3D-stacked
ence similarly to previously proposed in-memory data copy de-
                                                                                                                 memory described above.
signs [1][3]. To keep the memory system coherent, (1) the
                                                                                                                    Workloads. We evaluate NoM on four different benchmarks:
memory controller writes back the source block of a copy opera-
                                                                                                                 fork, one the most frequently-used system calls in operating sys-
tion if it is modified in cache (or all modified blocks from the
                                                                                                                 tems, fileCopy20, fileCopy40, and fileCopy60, three copy-intensive
source region, in case of bulk copy) before a copy, and (2) invali-
dates all cached blocks of the destination region present in cache.



===== Page 4 =====

 4                                                                                                                                            IEEE CAL



benchmarks that model the mcached memory object caching sys-
tem with different volumes of object copies [1]. Fig. 3 illustrates
the breakdown of memory accesses of each benchmark. The
memory access types are inter-bank and intra-bank copies, ini-
tialization, and regular read/write accesses from the program.
For these benchmarks, 20% to 60% of the memory traffic is gen-
erated by inter-bank copy operations. Note that processor-inten-
sive benchmarks, such as SPEC CPU, typically use a small num-
ber of page/data copies and hence, are not the target of NoM.
   Performance analysis. Fig. 4 compares the performance (IPC)
of the three evaluated memory configurations. We make three
observations. First, NoM provides 75% higher IPC than
RowClone on average, as it enables faster inter-bank copies and
performs multiple inter-bank copies and other memory accesses                     Fig. 3. Fraction of memory traffic due to different operations

concurrently. Second, increasing the data copy traffic rate be-
tween banks (by increasing the frequency of NoM links from 600
MHz to 1.25 GHz) provides more opportunity for NoM to exploit
its inherently high throughput, increasing the IPC benefits of
NoM over RowClone. Third, NoM-Light has only 5%-20% lower
IPC than baseline NoM, because of its more limited bandwidth
between layers. However, NoM-Light still greatly outperforms
RowClone.
   Energy analysis. To evaluate the energy consumption of NoM,
we use the DRAMPower simulator [14] and 3D-stacked DRAM
energy model from Micron [15]. Our experimental results show
that our mechanism reduces energy per access by up to 3.2x in
comparison with baseline DDR3 memory, as it eliminates costly                    Fig. 4. Performance of NoM and NoM-Light versus baselines
data exchange with the processor for copy operations. In compar-
ison to RowClone, NoM consumes up to 9% more energy, mainly            -based circuit- witching design with a central control mechanism
due to the extra links and logic it uses.                              implemented in the memory controller. Our experimental re-
    Area analysis. NoM equips each 16 MB DRAM bank with a              sults show an average of 75% and 3.8x performance improve-
simple router. We evaluate the area of NoM-Light using the area        ment over the state-of-the-art (RowClone) and a conventional 3D
models and parameters presented in [15] (for DRAM chips) and           DRAM architecture, respectively. NoM can be used in any
[16] (for TSVs). The results show that the area overhead of NoM        highly-banked memory, including the 3D-stacked high band-
(comprising buffers, slot table, crossbar, controller, links and       width memory (HBM) architecture.
TSVs) is negligible (below 1%) when compared to the area of a 16                                       REFERENCES
MB bank of HMC.                                                        [1] V. Seshadri et al., "RowClone: Fast and energy-efficient in-DRAM bulk
   Operating frequency. To traverse a network hop using circuit        data copy and initialization," in MICRO, 2013.
switching (which must be completed in a single cycle), the packet      [2] V. Seshadri, et al., “Ambit: in-memory accelerator for bulk bitwise oper-
should go through input latch read, crossbar traversal, link tra-      ations using commodity DRAM technology,” in MICRO, 2017.
versal, and input latch write in the downstream router. We model       [3] K. K. Chang, et al., “Low-Cost Inter-Linked Subarrays (LISA): Enabling
these datapath elements of NoM routers using CACTI in 45nm             fast inter-subarray data movement in DRAM,” in HPCA, 2016.
                                                                       [4] M. Gao et al., “DRAF: A Low-Power DRAM-Based Reconfigurable Ac-
DRAM technology to obtain the latency of each step. The results
                                                                       celeration Fabric,” in ISCA, 2016.
show that the total latency of a single hop is less than 300ps in      [5] V. Seshadri et al., "Fast Bulk Bitwise AND and OR in DRAM," in IEEE
45nm technology. Hence, our simple NoM routers are imple-              CAL, 2015.
mented in the DRAM layer, and the routers can keep up with the         [6] G. Kim et al., "Memory-centric system interconnect design with hybrid
speed of the logic layer, which is 1.25GHz. The critical path of the   memory cubes," in PACT, 2013.
hardware accelerator that allocates TDM slots (described in Sec-       [7] Hybrid Memory Cube Specification 2.1, Nov. 2015, Hybrid Memory
tion 2.2) is below 500ps, so it can find a path in a single cycle.     Cube Consortium, Tech. Rep.
                                                                       [8] T. Moscibroda et al.,”A Case for Bufferless Routing in On-chip Net-
    To investigate how NoM’s performance scales when NoM’s
                                                                       works ” in ISCA 2009.
link frequency is lower than DRAM frequency, we keep the logic         [9] F. Pakdaman, A. Mazloumi, M. Modarressi, “Integrated Circuit-Packet
layer frequency to 1.25GHz but reduce the frequency of NoM             Switching NoC with Efficient Circuit Setup Mechanism”, in Springer Journal
links by 25% and 50%. As Fig. 3 shows, NoM shows sublinear             of Supercomputing, vol. 71, no. 8, 2015.
performance degradation when NoM’s link frequency scales               [10] D. Lee et al., “Simultaneous Multi-Layer Access: Improving 3D-Stacked
down. However, NoM still delivers better performance than              Memory Bandwidth at Low Cost”, in TACO 2016.
RowClone because network latency comprises only part of the            [11] S. H. Seyyedaghaei, et al., “Dynamic Resource Sharing for High-Perfor-
copy latency, while a major source of copy latency is the bank         mance 3-D Networks-on-Chip”, in IEEE CAL, 2016.
                                                                       [12] S. H. Seyyedaghaei, et al., “Fault-Tolerant 3-D Network-on-Chip Deign
access latency. Furthermore, concurrent data transfers enabled
                                                                       using Dynamic Link Sharing”, in DATE, 2016.
by NoM can hide part of the link and bank latencies.                   [13] Y. Kim, et al., "Ramulator: A Fast and Extensible DRAM Simulator," in
                                                                       IEEE CAL, 2016.
 4 CONCLUSION                                                          [14] https://github.com/tukl-msd/DRAMPower
  This paper introduces NoM, a new design for performing di-           [15] Micron, DDR3 SDRAM system-power calculator, 2018.
                                                                       [16] D. Kim, et al., “Neurocube: A Programmable Digital Neuromorphic Ar-
rect data copy between banks inside a 3D-stacked DRAM chip.
                                                                       chitecture with High-Density 3D Memory,” in ISCA, 2016.
The proposed method uses a network between DRAM banks in               [17] Y. Kim et al., “A Case for Exploiting Subarray-Level Parallelism (SALP)
order to enable direct data transfer between them. NoM uses TDM        in DRAM”, in ISCA 2012.
